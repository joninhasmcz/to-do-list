version: '3.9'

services:
  db_postgres:
    image: postgres:alpine
    ports:
      - 5435:5432
    container_name: todolist_postgres
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: 2326252
      POSTGRES_DB: todolist-postgres-db